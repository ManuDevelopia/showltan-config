# HTTP Server
server:
  port: 3003   # HTTP port

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# Services
showltan:
  services:
    movies: http://localhost:3001/movies
    tvshows: http://localhost:3002/tvshows